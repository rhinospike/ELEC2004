\begin{tikzpicture}
	\begin{axis}[
		plot box ratio = 2,
		grid = both,
		xlabel=Frequency (Hz),
		ylabel=Gain (dB),
		xmode = log,
		scaled ticks=false,
		xticklabel={\pgfkeys{/pgf/fpu=true} \pgfmathparse{exp(\tick)} \pgfmathprintnumber[fixed relative, precision=3]{\pgfmathresult} \pgfkeys{/pgf/fpu=false}}
		]
		
	\addplot[color=red, mark=x] table [y= Gain] {Combined.dat};
	\addplot[color=blue] table [y= Expected] {Combined.dat};
	\legend{Recorded,Expected}
	\end{axis}
\end{tikzpicture}